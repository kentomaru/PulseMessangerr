# ⚡ Pulse — мессенджер

Мессенджер на Next.js 15: личные чаты, **группы и каналы с ролями**,
**групповые голосовые/видеозвонки (WebRTC mesh) со ссылкой-приглашением и
демонстрацией экрана**, **истории (24 ч)**, **обои чатов**, настройки
**приватности**, **закреплённые сообщения**, **поиск по чату**, **«Избранное»**,
**разметка сообщений (Markdown/спойлеры/упоминания)**, **эмодзи-пикер**,
ответы на сообщения, фото-сообщения, индикатор «печатает…», галочки
прочтения, онлайн-статусы, звук уведомлений.

## Возможности

| Функция | Как работает |
| --- | --- |
| 💬 Чаты | личные диалоги, поиск людей по @имени, непрочитанные, «печатает…», ✓/✓✓ прочтение, удаление сообщений |
| 📌 Закрепы | «Закрепить» в контекстном меню (или кнопкой у сообщения) — плашка под шапкой чата показывает до 10 последних; несколько закрепов листаются стрелками, клик по плашке перематывает к сообщению. В ЛС закрепать может любой, в группе/канале — владельцы, админы и автор сообщения |
| 🔍 Поиск по чату | лупа в шапке чата → панель с живым поиском (задержка 300 мс): по тексту сообщений, подписям и именам файлов, до 50 результатов с автором и временем; клик — переход к сообщению с подсветкой |
| 🔖 «Избранное» | закладка в сайдбаре → личный чат с самим собой: сохраняйте заметки и пересылайте туда сообщения («Переслать» → «Избранное»). Кнопок звонка там нет, подпись — «сохранённые сообщения» |
| ✨ Разметка | `**жирный**`, `*курсив*`, `__подчёркнутый__`, `~~зачёркнутый~~`, `` `код` ``, ```` ```блок кода``` ```` с кнопкой «копировать», `||спойлер||` (скрыт до клика), ссылки http(s) кликабельны, `@упоминания` подсвечиваются (своё — фиолетовым). Всё собирается в React-элементы без dangerouslySetInnerHTML, ссылки открываются с `rel="noopener noreferrer"` |
| 😀 Эмодзи-пикер | кнопка-смайл у поля ввода → 7 категорий (смайлы, жесты, сердца, праздники, животные, еда, символы); вставка в позицию курсора, закрытие по клику вне |
| ↩️ Разделитель «Непрочитанные» | при открытии чата с непрочитанными красная линия показывает, где начинаются новые сообщения |
| 🔔 Уведомления | звук нового сообщения (WebAudio, без файлов) + счётчик непрочитанных в заголовке вкладки `(3) Pulse`; звук и кнопка его выключения — в шапке сайдбара (настройка хранится в localStorage). Не пищит на первую загрузку и на сообщения в открытом чате, когда вкладка видна |
| ↩️ Ответы | кнопка «Ответить» у сообщения → цитата в пузыре, клик по цитате перематывает к оригиналу; если оригинал удалён, цитата показывает «Сообщение удалено» |
| 😀 Реакции | ПКМ (или долгое нажатие на телефоне) по сообщению → меню с быстрыми эмодзи; повторный клик по своей реакции снимает её. Кнопка «смайл» у сообщения открывает то же меню |
| ✏️ Редактирование | «Изменить» у своих текстовых сообщений и подписей к фото/файлам; у сообщения появляется пометка «изменено» |
| ➡️ Пересылка | «Переслать» в контекстном меню → выбор чата из списка; вложения пересылаются как есть |
| 🎤 Голосовые | кнопка микрофона в поле ввода → запись с таймером (до 5 мин), отмена или отправка. Плеер с «волной», перемоткой по клику и скоростью 1x/1.5x/2x |
| 🎬 Кружки | кнопка видеосообщения → запись до 60 секунд круглым кадром, предпросмотр, перезапись, отправка. Проигрывается в круглом пузыре с кольцом прогресса |
| 📎 Файлы | скрепка в поле ввода, drag&drop в область чата или Ctrl+V из буфера — **любые файлы до 500 МБ** (фото, видео, аудио, документы). Перед отправкой видно превью/имя/размер, можно подписать и только потом нажать «Отправить». В чате: фото с подписью, карточка файла со скачиванием, аудио/видео — с плеером |
| 🖱 Контекстное меню | ПКМ по сообщению (долгое нажатие на телефоне): реакции, ответить, копировать, изменить, переслать, удалить |
| 👥 Группы | создание с участниками, аватар, название, описание, роли **владелец / админ / участник**, добавление и исключение людей, выход (владение переходит следующему), удаление группы владельцем |
| 📢 Каналы | то же, что группы, но писать могут только администраторы; у подписчиков поле ввода закрыто |
| 🔎 Обзор | публичные группы и каналы ищутся в «Обзоре» (кнопка в сайдбаре) — с числом участников и кнопкой «Присоединиться»; приватные там не видны |
| 🔗 Ссылки-приглашения | у группы/канала постоянная ссылка `…/#group=<token>` — по ней входят даже в приватные |
| 📞 Звонки | аудио и видео через WebRTC: трафик идёт **напрямую между браузерами** (mesh: каждый участник соединён с каждым), сервер — только сигнальный центр (SDP/ICE в БД). В ЛС — дозвон («Звонит…» → принял/отклонил/пропущен), в группе/канале — комната, к которой можно присоединиться в любой момент. Лог звонка падает в чат: «Звонок · 2:31», «Пропущенный», «Отменённый». Состояние камеры («видео сразу при создании звонка») корректно сохраняется на сервере |
| 🖥 Демонстрация экрана | кнопка монитора в звонке (Discord-style): `getDisplayMedia` → видео-трек заменяется на экран у всех участников мгновенно (replaceTrack без пересогласования); если отправителя ещё не было — трек добавляется с renegotiation (оффер может пойти с любой стороны, glare разруливает perfect negotiation). Большая плитка 16:9 над сеткой участников, в мини-превью — что транслируется; остановка шаринга (кнопка или «Прекратить показ» в браузере) возвращает камеру |
| 🌍 Звонок «откуда угодно» | в окне звонка кнопка «скопировать ссылку» → `…/#join=<token>` (с фолбэком, если буфер обмена недоступен). Ссылку можно кинуть кому угодно: человек откроет её, увидит превью («Разработчики · 3 в звонке») и войдёт — даже если он не участник чата (в ЛС он останется гостем звонка, переписку не увидит; в публичной группе заодно станет участником). Второе средство — кнопка «+» в звонке: поиск по @имени и приглашение в текущий звонок |
| 🫧 «Динамический остров» | звонок сворачивается в перетаскиваемую пилюлю в углу: таймер, число участников, микрофон, «развернуть», «выйти». Чат, сайдбар и истории остаются доступны, звук продолжает идти |
| 📸 Истории | кружок «+» в сайдбаре → **фото или видео** (до 500 МБ) + подпись, живут 24 часа. В просмотре видно, **кто выложил** (клик по имени — карточка профиля), а автор видит список посмотревших; свои можно удалить |
| 🎨 Обои чатов | меню в шапке чата → 10 пресетов или своя картинка; у каждого участника свои |
| 🔒 Приватность | группа/канал — приватные (только по приглашению) или публичные (видны в «Обзоре»); у профиля: скрыть «в сети», запретить звонки, запретить новые чаты, выход со всех устройств, полное удаление аккаунта, страница `/privacy` |
| 🩺 Надёжность | retry-подключение к БД, healthcheck `/api/health`, подробные логи, идемпотентный перенос схемы при старте |

### Технические детали вложений

- **Загрузка** (`POST /api/upload`): файл льётся на диск **потоком** (без разворачивания 500 МБ в памяти), имя/тип/размер передаются в query; старый multipart-путь сохранён для совместимости.
- **Отдача** (`GET /api/files/[name]`): поддержан **HTTP Range** (перемотка аудио/видео), картинки/медиа — `inline`, остальное — `attachment` + `nosniff`.
- **Хранение в чате**: `messages.type = voice | video_note | file | image`, в `content` — JSON `{url, name, mimeType, size, duration, caption}`. Старые «сломанные» голосовые/кружки, сохранённые прошлой версией как `text` с JSON внутри, распознаются и отображаются плеерами автоматически.
- **Реакции**: таблица `message_reactions` (`message_id, user_id, emoji`, PK на тройке), белый список из 14 эмодзи на сервере, агрегация по эмодзи в выдаче (`{emoji, count, mine}`).

### Технические детали новых функций

- **Закрепы**: `messages.pinned_at` (timestamptz, null = не закреплено). `POST /api/messages/[id]/pin` — toggle, права: в ЛС любой участник, в группе/канале — owner/admin/автор. `GET /api/messages` отдаёт `pinned: ChatMessage[]` (до 10, свежие первыми).
- **Поиск**: `GET /api/messages/search?conversationId&q` — `ilike` по `content` (для вложений JSON парсится и ищется по caption/name), до 50 результатов с `{id, type, content, preview, createdAt, senderId, senderName, sender}`.
- **«Избранное»**: обычный личный чат, где единственный участник — вы сами (`POST /api/conversations {userId: <свой id>}` находит или создаёт). В списке диалогов помечается `saved: true` и называется «Избранное».
- **Разметка**: парсер в `src/lib/richText.tsx` — рекурсивный разбор инлайн-правил (самое левое совпадение побеждает), ```блоки кода``` вынимаются первыми. Спойлер и «копировать код» — компоненты с локальным состоянием. XSS невозможен по построению: никакого HTML, только React-элементы.
- **Демонстрация экрана**: `call_participants.screen_on` (boolean) — состояние участника; сам трек живёт в WebRTC (peer-соединения), сервер хранит только флаг для UI.
- **Звук уведомлений**: `src/lib/notify.ts` — два тона WebAudio (987 Гц + 1318 Гц) без аудиофайлов; флаг `localStorage.pulse_sound` (`on`/`off`).

## Быстрый старт (локально)

```bash
npm install
cp .env.example .env          # укажите DATABASE_URL
npm run dev                   # http://localhost:3000
```

Нужен PostgreSQL (14+). Таблицы создаются автоматически при старте сервера
(`ensureSchema` — идемпотентный DDL), отдельный шаг миграций не требуется:
старая схема звонков 1:1 переносится на комнаты один раз, старым группам
проставляется `kind` и владелец. Для ручной работы со схемой:
`npm run db:push` (drizzle-kit).

Переменные окружения:

| Переменная | Обязательна | Описание |
| --- | --- | --- |
| `DATABASE_URL` | да | строка подключения PostgreSQL, например `postgres://user:pass@host:5432/pulse` |
| `LOG_LEVEL` | нет | `debug` \| `info` (по умолчанию) \| `warn` \| `error` |
| `APP_URL` | нет | публичный адрес приложения — подставляется в ссылки-приглашения (`https://pulse.up.railway.app`). Без него ссылки собираются от текущего origin |
| `NEXT_PUBLIC_TURN_URL` | нет | TURN-сервер для звонков за строгим NAT, например `turn:turn.example.com:3478` (можно несколько через запятую) |
| `NEXT_PUBLIC_TURN_USERNAME` | нет | логин TURN |
| `NEXT_PUBLIC_TURN_CREDENTIAL` | нет | пароль TURN |

## Деплой на Railway

1. Создайте проект → **New → Database → PostgreSQL**.
2. Добавьте сервис из GitHub-репозитория.
3. В Variables сервиса приложения добавьте `DATABASE_URL` = `${{Postgres.DATABASE_URL}}`
   (Railway подставит приватную ссылку сам, если добавить Postgres в тот же проект).
4. Туда же полезно добавить `APP_URL` = публичный домен сервиса — тогда
   скопированные ссылки-приглашения сразу открываются у других людей.
5. Готово. `railway.json` уже настроен:
   - start: `npm run start`;
   - healthcheck: `/api/health` (щедрый таймаут 300 с);
   - при падении — до 10 автоперезапусков.

### Почему сервер больше не падает, если база стартует позже приложения

Раньше код обращался к БД на этапе запуска, и если Postgres ещё не поднялся,
процесс падал. Теперь:

- при старте `src/instrumentation.ts` ждёт базу: **попытка → 2 секунды → новая попытка**;
- первые ~16 секунд — в блокирующем режиме (обычно база готова за 2–4 с);
- если база всё ещё поднимается — сервер **продолжает работать**, ждёт в фоне,
  страница показывает экран «Подключаемся к базе данных» (сам обновится),
  API отвечают `503` и **повторяют запрос через 2 секунды** автоматически;
- когда база отвечает — схема создаётся/дополняется автоматически, всё
  начинает работать без перезапуска;
- healthcheck честно возвращает `200` только когда база жива.

Дополнительно: Next.js собирает `instrumentation.ts` и для edge-слоя, где
нет Node-модулей (`net`, `tls`), нужных драйверу `postgres`. В `next.config.ts`
драйвер и Node-модули помечены внешними для edge-целей — без этого любой
запрос в `next dev` падал с `Module not found: Can't resolve 'net'`.

⚠️ Ловушка (уже была): externals должны применяться **только** при
`nextRuntime === "edge"`. В Next 15 `nextRuntime === undefined` — это
**клиентская** (браузерная) сборка. Если externals (`process`, `net`, `crypto`…)
попадают туда, в браузерный бандл уезжает `require("process")`: `require` в
браузере не существует, `ReferenceError` роняет весь клиентский JS ещё до
гидратации React. Симптом обманчивый — сервер отвечает 200, данные в БД
целы, а в браузере виден только SSR-каркас: сайдбар пустой, чаты не
открываются, кнопки не реагируют. Поэтому `npm run smoke` (см. ниже) —
обязательная часть проверки перед деплоем.

### Ошибка сборки «EBUSY … rmdir /app/node_modules/.cache»

Симптом: деплой падает на этапе сборки с

```
npm error code EBUSY
npm error syscall rmdir
npm error path /app/node_modules/.cache
npm error EBUSY: resource busy or locked, rmdir '/app/node_modules/.cache'
failed to solve: process "/bin/bash -ol pipefail -c npm ci … did not complete successfully"
```

Причина не в коде: билд-демон Railway подмонтирует кэш сборки в
`/app/node_modules/.cache` (Docker держит эту директорию залоченной), а
`npm ci` **перед установкой сносит весь `node_modules`** — и упирается в
заблокированную точку монтирования.

Фикс (уже применён): в `railway.json` вместо `npm ci` стоит `npm install`,
который не удаляет дерево, а только доустанавливает недостающее:

```json
"buildCommand": "npm install --include=dev --include=optional --no-audit --no-fund && npm run build"
```

Флаги тут важны:

- `--include=dev` — в окружении сборки Railway встречается `production=true`
  (в логе это выглядит как `npm warn config production Use --omit=dev instead`),
  из-за чего npm пропускает devDependencies. А `next build` без них не живёт:
  нужны `typescript`, `tailwindcss` и `@tailwindcss/postcss`.
- `--include=optional` — ставит платформенные бинарники Tailwind v4
  (`@tailwindcss/oxide-*`, `lightningcss-*`), иначе сборка падает с
  «Cannot find native binding».

Если ошибка всё же повторится (кэш сборки может «залипнуть»), помогает
`Redeploy` того же коммита или переменная `NO_CACHE=1` в Variables сервиса —
она отключает кэширование слоёв, и `/app/node_modules/.cache` больше не
монтируется.

Проверить локально, что сборка самодостаточна:

```bash
git clone <repo> /tmp/rw && cd /tmp/rw
npm install --include=dev --include=optional --no-audit --no-fund && npm run build
```

## Логи: «где что сломалось?»

Все логи — структурированные, в stdout (Railway их показывает в Deploy Logs):

```
[2026-09-11T13:40:22.290Z] [INFO] [api:messages] POST /api/messages → 200 ms=7
[2026-09-11T13:40:22.290Z] [INFO] [calls] Участник вошёл в звонок callId=… userId=… participants=3 guest=false
[2026-09-11T13:40:22.290Z] [WARN] [db] База данных недоступна (попытка 3/45). Повтор через 2 с err=ECONNREFUSED
[2026-09-11T13:42:38.196Z] [ERROR] [api:conversations] GET /api/conversations: необработанная ошибка err={...stack...}
```

- `[api:...]` — каждый HTTP-запрос: метод, путь, статус, длительность;
- `[calls]` — жизненный цикл звонков: кто вошёл/вышел, почему звонок завершился;
- `[db]` / `[startup]` — подключение базы и старт сервера;
- `[server]` — все неперехваченные ошибки Next.js (`onRequestError`);
- в логи **не** пишутся пароли, содержимое сообщений и SDP-описания звонков.

Поиск проблемы: `Ошибка` / `ERROR` → scope в квадратных скобках показывает,
какой роут/модуль упал, `ms=` помогает найти медленные запросы.

## Архитектура

```
src/
├── app/
│   ├── page.tsx              — вход/мессенджер (SSR)
│   ├── privacy/page.tsx      — политика конфиденциальности
│   └── api/
│       ├── auth/*            — регистрация, вход, профиль, logout-all, удаление аккаунта
│       ├── users/*           — поиск людей, карточка пользователя
│       ├── conversations/*   — список/создание чатов, групп и каналов, настройки,
│       │                       участники и роли, ссылки-приглашения, join,
│       │                       прочтение, typing, обои
│       ├── discover          — «Обзор»: публичные группы и каналы
│       ├── messages/*        — отправка/получение/удаление сообщений, ответы
│       ├── calls/*           — комнаты: start, incoming, [id] (join|signal|state|
│       │                       invite|leave|end|decline), [id]/invite (ссылка),
│       │                       info/[token] (превью), join/[token] (вход «с улицы»)
│       ├── stories/*         — истории: лента, публикация, просмотры, удаление
│       ├── upload + files    — загрузка/раздача изображений
│       └── health            — healthcheck для Railway
├── components/               — UI: Sidebar, ChatView, CallStage (окно звонка +
│                               «динамический остров»), GroupCreateModal,
│                               GroupInfoModal, DiscoverModal, PeoplePicker,
│                               Stories*, Profile, UserCard, Wallpaper
├── lib/
│   ├── useCallController.ts  — клиентская логика mesh-звонков (WebRTC + опрос комнаты)
│   ├── calls.ts              — серверная часть звонков: комнаты, участники, сигналы, логи
│   ├── conversations.ts      — роли/доступы, сериализация чатов, ссылки-приглашения
│   ├── api-helpers.ts        — withApi: авторизация + логи + retry БД + единые ошибки
│   └── logger.ts             — структурированный логгер
├── db/                       — drizzle-схема + подключение с retry + ensureSchema
└── instrumentation.ts        — стартовый hook: ожидание БД + ensureSchema + onRequestError
```

Реальное время — короткий опрос (1.2–4 с): чаты каждые 4 с, сообщения 2.5 с,
входящий звонок 3 с, состояние активного звонка 1.2 с (этот же запрос —
heartbeat: если участник молчит 90 с, сервер считает его выбывшим). Для
небольшого числа пользователей этого достаточно, а WebSocket-сервер не требуется.

### Как устроены звонки

- **Комната на диалог**: `calls` (статус `ringing`/`live`/`ended`/`missed`/`declined`,
  `join_token`), `call_participants` (кто в комнате, SDP, флаги микрофона/камеры,
  гость ли), `call_signals` (offer/answer/ice очередью адресату), `call_invites`
  (кого позвали — он увидит звонок во входящих).
- **ЛС**: звонок `ringing`, становится `live` когда второй принял, завершается
  как только кто-то вышел. **Группа/канал**: комната `live` сразу, живёт пока
  в ней есть люди; опоздавшие видят в шапке чата «В звонке · N» и входят.
- **Соединение**: mesh, у каждого участника по `RTCPeerConnection` с каждым.
  Оффер шлёт детерминированно «меньший» по id — встречные офферы (glare)
  разруливаются откатом, как в perfect negotiation. Если ответ потерялся,
  оффер переотправляется через 6 с.
- **Кто может войти**: участник диалога → приглашённый (`call_invites`) →
  любой по `join_token` (гость). В приватном ЛС гость видит только звонок.

## Звонки и группы: как проверить

Откройте мессенджер в двух окнах (обычное + инкогнито) под разными аккаунтами:

1. **ЛС-звонок**: создайте чат, нажмите 📞 или 📹 → во втором окне появится
   входящий. Разрешите доступ к микрофону/камере.
2. **Группа**: «+» в сайдбаре → «Создать группу» (или канал), добавьте людей,
   в карточке группы (клик по шапке) — роли, ссылка-приглашение, приватность.
3. **Групповой звонок**: в группе нажмите 📞 — комната живая сразу; второй
   участник видит её во входящих и в шапке чата («В звонке · 1»).
4. **Ссылка «откуда угодно»**: в окне звонка — кнопка «скопировать ссылку»,
   откройте её в третьем окне под другим аккаунтом (или отправьте любому
   человеку): появится превью и вход в звонок.
5. **«Динамический остров»**: кнопка «свернуть» в окне звонка — пилюлю можно
   перетащить, звук и чат продолжают работать.
6. **Ответы**: наведите на сообщение → «Ответить»; клик по цитате — переход к
   оригиналу.
7. **Истории**: «+» в сайдбаре → публикация; в просмотре кликните по имени
   автора — откроется его карточка.

Звонок соединяется напрямую (P2P). Используется публичный STUN; если сеть
блокирует P2P (симметричный NAT), задайте свой TURN через
`NEXT_PUBLIC_TURN_URL` / `NEXT_PUBLIC_TURN_USERNAME` / `NEXT_PUBLIC_TURN_CREDENTIAL`.

## Проверки

```bash
npm run typecheck   # tsc --noEmit — типы
npm run build       # prod-сборка
npm run smoke       # UI-смок: настоящий клиентский бандл в jsdom + клики
```

`npm run smoke` — UI-проверка, которой раньше не хватало: API-проверки не
видят «мёртвый» интерфейс (см. ловушку с externals выше). Скрипт
(`scripts/ui-smoke.mjs`, jsdom в devDeps) сам регистрирует двух пользователей
и проходит сценарий **через реальный клиентский бандл**:

гидратация React → сайдбар → поиск людей → меню «+» → создание группы →
открытие чата → отправка сообщения → меню «⋮» → модалка обоев → её закрытие
(exit-анимация AnimatePresence) → модалка профиля. Везде перехватываются
`window.onerror`, `unhandledrejection` и `console.error` — любая ошибка
рантайма клиента тоже считается провалом.

Запускать против запущенного сервера (dev ИЛИ prod — смок не различает):

```bash
npm run dev                          # терминал 1
npm run smoke                        # терминал 2 — ✅ 10 / ❌ 0
# или prod:
npm run build && npm run start       # терминал 1
npm run smoke                        # терминал 2
```

Адрес сервера — `SMOKE_URL` (по умолчанию `http://127.0.0.1:3000`); таймауты
`SMOKE_HEALTH_TIMEOUT_MS` (ожидание `/api/health`, по умолчанию 240 с — в dev
первая компиляция дольше) и `SMOKE_TIMEOUT_MS` (весь сценарий, 300 с).
Код выхода 0 только если все 10 проверок зелёные и ошибок рантайма клиента
нет — удобно в CI.

## Заметки

- Загруженные файлы хранятся в `data/uploads` (на Railway диск ephemeral —
  при пересоздании контейнера картинки пропадут; для постоянного хранения
  подключите S3-совместимое хранилище и замените `/api/upload`).
- Пароли — scrypt-хэш, сессии — httpOnly-cookie на 30 дней.
- Mesh-звонки — это N·(N−1)/2 соединений: для 4–6 человек комфортно, для
  больших собраний нужен SFU (LiveKit/mediasoup).
